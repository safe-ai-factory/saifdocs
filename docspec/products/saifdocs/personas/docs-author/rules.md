# Docs-author writing rules

- Show exact CLI flags with their defaults.
- Never assume the reader knows what saifctl is; link the concept on first mention.
- Use concrete file paths (e.g. `docspec/products/myapp/product.md`) over abstract descriptions.
- Lead with the minimal working example before explaining all options.
